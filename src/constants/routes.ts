enum Route {
  Login = '/login',
  Logout = '/logout',
  TimeTrack = '/tracker',
  Periods = '/periods',
  DashBoard = '/dashboard',
  Reports = '/reports',
  Projects = '/projects',
  Team = '/team',
  Clients = '/clients',
  OrganizationSettings = '/settings',
  ProfileSettings = '/settings/account',
}

export { Route };

